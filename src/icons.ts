import { addIcons } from 'ionicons';
import { cameraOutline, shareSocialOutline } from 'ionicons/icons';

// Registra los iconos que vas a usar por nombre
addIcons({
  'camera-outline': cameraOutline,
  'share-social-outline': shareSocialOutline
});
